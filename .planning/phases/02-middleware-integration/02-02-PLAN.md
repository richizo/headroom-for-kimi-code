---
phase: 02-middleware-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/test_proxy/test_moonshot_cache.py
  - tests/test_proxy/test_moonshot_compression.py
  - tests/test_proxy/test_moonshot_telemetry.py
  - headroom/backends/moonshot.py (possíveis ajustes em cache/usage)
autonomous: true
requirements:
  - INTG-01
  - TEST-03
  - TEST-04
user_setup: []
must_haves:
  truths:
    - Cache do proxy funciona com Moonshot (PrefixCacheTracker.update_from_response)
    - Compressão é aplicada a requisições Moonshot longas
    - Telemetria registra provider="moonshot" e tokens corretos
  artifacts:
    - tests/test_proxy/test_moonshot_cache.py
    - tests/test_proxy/test_moonshot_compression.py
    - tests/test_proxy/test_moonshot_telemetry.py
  key_links:
    - PrefixCacheTracker.update_from_response
    - CompressionDecision / openai_pipeline.apply
    - RequestOutcome / emit_request_outcome
---

<objective>
Validar e garantir que cache, compressão e telemetria do Headroom se aplicam a chamadas Moonshot.

Purpose: Confirmar que as capacidades de valor do Headroom funcionam com o backend Moonshot sem quebrar o comportamento existente.
Output: Testes focados em cache, compressão e telemetria para Moonshot; ajustes mínimos no backend se necessário.
</objective>

<execution_context>
@$HOME/.cline/gsd-core/workflows/execute-plan.md
@$HOME/.cline/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-middleware-integration/02-CONTEXT.md
@.planning/phases/02-middleware-integration/02-RESEARCH.md
@.planning/phases/02-middleware-integration/02-01-PLAN.md
@.planning/phases/01-backend-moonshot-non-streaming/01-03-SUMMARY.md

@headroom/backends/moonshot.py
@headroom/cache/prefix_tracker.py
@headroom/proxy/handlers/openai.py
@headroom/proxy/outcome.py
@tests/test_proxy/test_openai_backend_path.py
@tests/test_proxy_openai_cache_stability.py
@tests/test_request_outcome.py
</context>

<tasks>

<task type="auto">
  <name>Validar integração de cache</name>
  <files>tests/test_proxy/test_moonshot_cache.py</files>
  <read_first>tests/test_proxy/test_openai_backend_path.py, headroom/cache/prefix_tracker.py</read_first>
  <action>
    Criar `tests/test_proxy/test_moonshot_cache.py`:

    1. Reutilizar o padrão de `test_openai_backend_path.py` para criar proxy com backend="moonshot" e instalar stub do `PrefixCacheTracker`.

    2. `test_moonshot_cache_read_and_creation_tokens_update_tracker`:
       - Mock de `send_openai_message` retorna resposta com:
         ```python
         usage = {
             "prompt_tokens": 1000,
             "completion_tokens": 20,
             "total_tokens": 1020,
             "cache_read_input_tokens": 700,
             "cache_creation_input_tokens": 100,
             "prompt_tokens_details": {"cached_tokens": 700},
         }
         ```
       - POST `/v1/chat/completions` com `model=kimi-k2`.
       - Assert que `tracker.calls[0]["cache_read_tokens"] == 700` e `cache_write_tokens == 100`.

    3. `test_moonshot_falls_back_to_openai_cached_tokens`:
       - Mock retorna resposta apenas com `prompt_tokens_details.cached_tokens` (sem top-level Anthropic keys).
       - Assert que `cache_read_tokens` usa o fallback e `cache_write_tokens` é inferido (`prompt_tokens - cache_read`).

    4. Se a API Moonshot retornar campos de cache com nomes diferentes (e.g. `cached_tokens`), adicionar mapeamento em `MoonshotBackend.send_openai_message` para normalizar para o formato OpenAI esperado pelo handler.
  </action>
  <verify>
    Run: `python -m pytest tests/test_proxy/test_moonshot_cache.py -v`
  </verify>
  <acceptance_criteria>
    - `PrefixCacheTracker.update_from_response` é chamado com valores corretos
    - Fallback OpenAI funciona
  </acceptance_criteria>
  <done>
    Cache integration validated.
  </done>
</task>

<task type="auto">
  <name>Validar integração de compressão</name>
  <files>tests/test_proxy/test_moonshot_compression.py</files>
  <read_first>headroom/proxy/compression_decision.py, headroom/transforms/pipeline.py, tests/test_proxy_compress_endpoint.py</read_first>
  <action>
    Criar `tests/test_proxy/test_moonshot_compression.py`:

    1. Criar proxy com `optimize=True`, `cache_enabled=False`, `rate_limit_enabled=False`.

    2. Substituir transporte do backend real para capturar o body otimizado enviado upstream.

    3. `test_compression_is_applied_to_long_moonshot_request`:
       - Enviar request com mensagens longas (e.g. JSON repetitivo ou texto grande) que ativem compressão.
       - Assert que o body capturado upstream difere do body original (mensagens foram comprimidas).
       - Assert que `input_tokens_optimized < input_tokens_original` no telemetry/cost.

    4. `test_short_moonshot_request_is_not_compressed`:
       - Enviar request curto.
       - Assert que o body upstream é igual ao original.

    5. Se necessário, ajustar `MoonshotBackend.send_openai_message` para garantir que não reverta mensagens comprimidas (não deve; backend é pass-through).
  </action>
  <verify>
    Run: `python -m pytest tests/test_proxy/test_moonshot_compression.py -v`
  </verify>
  <acceptance_criteria>
    - Compressão é acionada para requests longos
    - Requests curtos não são comprimidos
  </acceptance_criteria>
  <done>
    Compression integration validated.
  </done>
</task>

<task type="auto">
  <name>Validar integração de telemetria</name>
  <files>tests/test_proxy/test_moonshot_telemetry.py</files>
  <read_first>headroom/proxy/outcome.py, headroom/proxy/request_logger.py, tests/test_request_outcome.py, tests/test_proxy_stats_recent_requests.py</read_first>
  <action>
    Criar `tests/test_proxy/test_moonshot_telemetry.py`:

    1. Criar proxy com backend="moonshot", com `request_logger` ativo.

    2. `test_moonshot_request_logs_provider_and_tokens`:
       - Mock retorna resposta OpenAI com `usage`.
       - POST `/v1/chat/completions`.
       - Acessar `proxy.request_logger.get_recent_with_messages(1)` e assert `provider == "moonshot"`, `model == "kimi-k2"`, tokens corretos.

    3. `test_moonshot_appears_in_stats`:
       - Fazer uma ou mais requisições Moonshot.
       - GET `/stats`.
       - Assert que o response contém provider "moonshot" ou dados agregados com tokens da Moonshot.

    4. `test_moonshot_request_emits_prometheus_metrics`:
       - Se possível, verificar que `proxy.metrics` (PrometheusMetrics) registrou a requisição com provider="moonshot".

    5. Não modificar `MoonshotBackend`; apenas validar que `name == "moonshot"` já é suficiente.
  </action>
  <verify>
    Run: `python -m pytest tests/test_proxy/test_moonshot_telemetry.py -v`
  </verify>
  <acceptance_criteria>
    - Request logger registra provider="moonshot"
    - Tokens corretos são capturados
    - `/stats` reflete Moonshot
  </acceptance_criteria>
  <done>
    Telemetry integration validated.
  </done>
</task>

<task type="auto">
  <name>Executar suite de regressão</name>
  <files></files>
  <read_first></read_first>
  <action>
    Rodar suites relacionadas para garantir que novos testes e eventuais ajustes não quebraram backends existentes:
    - `python -m pytest tests/test_proxy/test_openai_backend_path.py -v`
    - `python -m pytest tests/test_request_outcome.py -v`
    - `python -m pytest tests/test_proxy/test_request_logger.py -v`
    - `python -m pytest tests/test_backends/test_moonshot.py -v`
  </action>
  <verify>
    All regression tests pass.
  </verify>
  <acceptance_criteria>
    - Nenhum teste existente quebra
  </acceptance_criteria>
  <done>
    Regression suite passed.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/test_proxy/test_moonshot_cache.py -v` passes
- [ ] `python -m pytest tests/test_proxy/test_moonshot_compression.py -v` passes
- [ ] `python -m pytest tests/test_proxy/test_moonshot_telemetry.py -v` passes
- [ ] Regressão: testes relacionados continuam passando
- [ ] `ruff check` passa nos novos arquivos
- [ ] `mypy` passa (ou apenas erros pré-existentes)
</verification>

<success_criteria>
- Cache, compressão e telemetria funcionam com Moonshot
- Nenhum backend existente é impactado
- Testes cobrem as três capacidades de valor
</success_criteria>

<output>
After completion, create `.planning/phases/02-middleware-integration/02-02-SUMMARY.md`
</output>
